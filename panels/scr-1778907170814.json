{
  "screenId": "scr-1778907170814",
  "screenName": "異常値検出ログ画面",
  "html": "<div style=\"padding: 12px; background: var(--bg-main); color: var(--text);\">\n  <div style=\"display: flex; align-items: center; justify-content: space-between; margin-bottom: 12px;\">\n    <h1 style=\"font-size: 18px; font-weight: 600; margin: 0; color: var(--text);\">異常値検出ログ</h1>\n    <button id=\"btn-export\" class=\"action-btn secondary\" style=\"background: transparent; color: var(--text); border: 1px solid rgba(255,255,255,0.18); padding: 6px 14px; border-radius: 6px; font-size: 13px; cursor: pointer;\">CSVエクスポート</button>\n  </div>\n\n  <div class=\"card\" style=\"background: var(--bg-card); border: 1px solid var(--border); border-radius: 8px; padding: 14px; margin-bottom: 12px;\">\n    <h2 style=\"font-size: 14px; font-weight: 600; margin: 0 0 12px 0; color: var(--text);\">検索条件</h2>\n    <div style=\"display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 12px; margin-bottom: 12px;\">\n      <div>\n        <label style=\"display: block; font-size: 11px; color: var(--text-muted); margin-bottom: 4px;\">検出日時（開始）</label>\n        <input type=\"date\" id=\"input-date-start\" style=\"padding: 6px 10px; border: 1px solid rgba(255,255,255,0.18); border-radius: 6px; background: rgba(0,0,0,0.25); color: var(--text); font-size: 13px; line-height: 1.4; width: 100%; box-sizing: border-box;\">\n      </div>\n      <div>\n        <label style=\"display: block; font-size: 11px; color: var(--text-muted); margin-bottom: 4px;\">検出日時（終了）</label>\n        <input type=\"date\" id=\"input-date-end\" style=\"padding: 6px 10px; border: 1px solid rgba(255,255,255,0.18); border-radius: 6px; background: rgba(0,0,0,0.25); color: var(--text); font-size: 13px; line-height: 1.4; width: 100%; box-sizing: border-box;\">\n      </div>\n      <div>\n        <label style=\"display: block; font-size: 11px; color: var(--text-muted); margin-bottom: 4px;\">異常値種別</label>\n        <select id=\"select-anomaly-type\" style=\"padding: 6px 10px; border: 1px solid rgba(255,255,255,0.18); border-radius: 6px; background: rgba(0,0,0,0.25); color: var(--text); font-size: 13px; line-height: 1.4; width: 100%; box-sizing: border-box;\">\n          <option value=\"\">全て</option>\n          <option value=\"長時間作業\">長時間作業</option>\n          <option value=\"頻繁中断\">頻繁中断</option>\n          <option value=\"工数超過\">工数超過</option>\n          <option value=\"異常な開始時刻\">異常な開始時刻</option>\n          <option value=\"異常な終了時刻\">異常な終了時刻</option>\n        </select>\n      </div>\n      <div>\n        <label style=\"display: block; font-size: 11px; color: var(--text-muted); margin-bottom: 4px;\">作業員名</label>\n        <input type=\"text\" id=\"input-worker-name\" placeholder=\"作業員名で検索\" style=\"padding: 6px 10px; border: 1px solid rgba(255,255,255,0.18); border-radius: 6px; background: rgba(0,0,0,0.25); color: var(--text); font-size: 13px; line-height: 1.4; width: 100%; box-sizing: border-box;\">\n      </div>\n      <div>\n        <label style=\"display: block; font-size: 11px; color: var(--text-muted); margin-bottom: 4px;\">重要度</label>\n        <select id=\"select-severity\" style=\"padding: 6px 10px; border: 1px solid rgba(255,255,255,0.18); border-radius: 6px; background: rgba(0,0,0,0.25); color: var(--text); font-size: 13px; line-height: 1.4; width: 100%; box-sizing: border-box;\">\n          <option value=\"\">全て</option>\n          <option value=\"高\">高</option>\n          <option value=\"中\">中</option>\n          <option value=\"低\">低</option>\n        </select>\n      </div>\n      <div>\n        <label style=\"display: block; font-size: 11px; color: var(--text-muted); margin-bottom: 4px;\">確認状況</label>\n        <select id=\"select-status\" style=\"padding: 6px 10px; border: 1px solid rgba(255,255,255,0.18); border-radius: 6px; background: rgba(0,0,0,0.25); color: var(--text); font-size: 13px; line-height: 1.4; width: 100%; box-sizing: border-box;\">\n          <option value=\"\">全て</option>\n          <option value=\"未確認\">未確認</option>\n          <option value=\"確認済み\">確認済み</option>\n          <option value=\"対応済み\">対応済み</option>\n        </select>\n      </div>\n    </div>\n    <div style=\"display: flex; gap: 8px;\">\n      <button id=\"btn-search\" class=\"action-btn\" style=\"background: var(--primary); color: #fff; border: none; padding: 6px 14px; border-radius: 6px; font-size: 13px; font-weight: 500; cursor: pointer; transition: background 0.15s;\">検索</button>\n      <button id=\"btn-clear\" class=\"action-btn secondary\" style=\"background: transparent; color: var(--text); border: 1px solid rgba(255,255,255,0.18); padding: 6px 14px; border-radius: 6px; font-size: 13px; cursor: pointer;\">クリア</button>\n    </div>\n  </div>\n\n  <div class=\"card\" style=\"background: var(--bg-card); border: 1px solid var(--border); border-radius: 8px; padding: 14px;\">\n    <h2 style=\"font-size: 14px; font-weight: 600; margin: 0 0 12px 0; color: var(--text);\">異常値検出ログ一覧</h2>\n    <div class=\"table-wrap\" style=\"overflow-x: auto;\">\n      <table style=\"width: 100%; border-collapse: collapse; font-size: 13px;\">\n        <thead>\n          <tr style=\"background: rgba(255,255,255,0.02);\">\n            <th style=\"padding: 6px 10px; text-align: left; border-bottom: 1px solid var(--border); color: var(--text); font-weight: 600;\">検出日時</th>\n            <th style=\"padding: 6px 10px; text-align: left; border-bottom: 1px solid var(--border); color: var(--text); font-weight: 600;\">作業員名</th>\n            <th style=\"padding: 6px 10px; text-align: left; border-bottom: 1px solid var(--border); color: var(--text); font-weight: 600;\">異常値種別</th>\n            <th style=\"padding: 6px 10px; text-align: left; border-bottom: 1px solid var(--border); color: var(--text); font-weight: 600;\">検出項目</th>\n            <th style=\"padding: 6px 10px; text-align: left; border-bottom: 1px solid var(--border); color: var(--text); font-weight: 600;\">検出値</th>\n            <th style=\"padding: 6px 10px; text-align: left; border-bottom: 1px solid var(--border); color: var(--text); font-weight: 600;\">閾値</th>\n            <th style=\"padding: 6px 10px; text-align: left; border-bottom: 1px solid var(--border); color: var(--text); font-weight: 600;\">重要度</th>\n            <th style=\"padding: 6px 10px; text-align: left; border-bottom: 1px solid var(--border); color: var(--text); font-weight: 600;\">確認状況</th>\n            <th style=\"padding: 6px 10px; text-align: left; border-bottom: 1px solid var(--border); color: var(--text); font-weight: 600;\">操作</th>\n          </tr>\n        </thead>\n        <tbody id=\"log-table-body\">\n        </tbody>\n      </table>\n    </div>\n  </div>\n\n  <div id=\"detail-panel\" class=\"card hidden\" style=\"background: var(--bg-card); border: 1px solid var(--border); border-radius: 8px; padding: 14px; margin-top: 12px;\">\n    <h2 style=\"font-size: 14px; font-weight: 600; margin: 0 0 12px 0; color: var(--text);\">異常値詳細・対応</h2>\n    <div style=\"display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-bottom: 12px;\">\n      <div>\n        <label style=\"display: block; font-size: 11px; color: var(--text-muted); margin-bottom: 4px;\">確認状況</label>\n        <select id=\"select-detail-status\" style=\"padding: 6px 10px; border: 1px solid rgba(255,255,255,0.18); border-radius: 6px; background: rgba(0,0,0,0.25); color: var(--text); font-size: 13px; line-height: 1.4; width: 100%; box-sizing: border-box;\">\n          <option value=\"未確認\">未確認</option>\n          <option value=\"確認済み\">確認済み</option>\n          <option value=\"対応済み\">対応済み</option>\n        </select>\n      </div>\n      <div>\n        <label style=\"display: block; font-size: 11px; color: var(--text-muted); margin-bottom: 4px;\">確認者</label>\n        <input type=\"text\" id=\"input-reviewer\" readonly style=\"padding: 6px 10px; border: 1px solid rgba(255,255,255,0.18); border-radius: 6px; background: rgba(0,0,0,0.15); color: var(--text-muted); font-size: 13px; line-height: 1.4; width: 100%; box-sizing: border-box;\">\n      </div>\n    </div>\n    <div style=\"margin-bottom: 12px;\">\n      <label style=\"display: block; font-size: 11px; color: var(--text-muted); margin-bottom: 4px;\">対応メモ</label>\n      <textarea id=\"textarea-memo\" rows=\"3\" placeholder=\"対応内容や備考を入力してください\" style=\"padding: 6px 10px; border: 1px solid rgba(255,255,255,0.18); border-radius: 6px; background: rgba(0,0,0,0.25); color: var(--text); font-size: 13px; line-height: 1.4; width: 100%; box-sizing: border-box; resize: vertical;\"></textarea>\n    </div>\n    <div style=\"display: flex; gap: 8px;\">\n      <button id=\"btn-update-status\" class=\"action-btn\" style=\"background: var(--primary); color: #fff; border: none; padding: 6px 14px; border-radius: 6px; font-size: 13px; font-weight: 500; cursor: pointer; transition: background 0.15s;\">更新</button>\n      <button id=\"btn-close-detail\" class=\"action-btn secondary\" style=\"background: transparent; color: var(--text); border: 1px solid rgba(255,255,255,0.18); padding: 6px 14px; border-radius: 6px; font-size: 13px; cursor: pointer;\">閉じる</button>\n      <button id=\"btn-view-record\" class=\"action-btn secondary\" style=\"background: transparent; color: var(--text); border: 1px solid rgba(255,255,255,0.18); padding: 6px 14px; border-radius: 6px; font-size: 13px; cursor: pointer;\">作業記録を確認</button>\n    </div>\n  </div>\n</div>",
  "initJs": "function(container, apiUrl, tables) {\n  const tbody = container.querySelector(\"#log-table-body\");\n  const detailPanel = container.querySelector(\"#detail-panel\");\n  let currentLogId = null;\n  let allLogs = [];\n\n  // 重要度バッジのスタイル\n  function getSeverityBadge(severity) {\n    const colors = {\n      '高': 'background: #dc2626; color: #fff;',\n      '中': 'background: #f59e0b; color: #fff;',\n      '低': 'background: #10b981; color: #fff;'\n    };\n    return `<span style=\"padding: 2px 6px; border-radius: 4px; font-size: 11px; font-weight: 500; ${colors[severity] || 'background: var(--bg-main); color: var(--text);'}\">${severity || ''}</span>`;\n  }\n\n  // 確認状況バッジのスタイル\n  function getStatusBadge(status) {\n    const colors = {\n      '未確認': 'background: #ef4444; color: #fff;',\n      '確認済み': 'background: #f59e0b; color: #fff;',\n      '対応済み': 'background: #10b981; color: #fff;'\n    };\n    return `<span style=\"padding: 2px 6px; border-radius: 4px; font-size: 11px; font-weight: 500; ${colors[status] || 'background: var(--bg-main); color: var(--text);'}\">${status || ''}</span>`;\n  }\n\n  // 1. テーブル再描画関数を定義\n  function renderRows(rows) {\n    tbody.innerHTML = rows.map(r => `\n      <tr data-id=\"${r.id}\" style=\"border-bottom: 1px solid var(--border); cursor: pointer;\" class=\"log-row\">\n        <td style=\"padding: 6px 10px; color: var(--text);\">${r.検出日時 ? new Date(r.検出日時).toLocaleString('ja-JP') : ''}</td>\n        <td style=\"padding: 6px 10px; color: var(--text);\">${r.作業員名 || ''}</td>\n        <td style=\"padding: 6px 10px; color: var(--text);\">${r.異常値種別 || ''}</td>\n        <td style=\"padding: 6px 10px; color: var(--text);\">${r.検出項目 || ''}</td>\n        <td style=\"padding: 6px 10px; color: var(--text);\">${r.検出値 || ''}</td>\n        <td style=\"padding: 6px 10px; color: var(--text);\">${r.閾値 || ''}</td>\n        <td style=\"padding: 6px 10px;\">${getSeverityBadge(r.重要度)}</td>\n        <td style=\"padding: 6px 10px;\">${getStatusBadge(r.確認状況)}</td>\n        <td style=\"padding: 6px 10px;\">\n          <button class=\"action-btn secondary detail-btn\" data-id=\"${r.id}\" style=\"background: transparent; color: var(--text); border: 1px solid rgba(255,255,255,0.18); padding: 4px 8px; border-radius: 4px; font-size: 11px; cursor: pointer;\">詳細</button>\n        </td>\n      </tr>\n    `).join(\"\");\n  }\n\n  // 2. 初期データを即座に描画\n  allLogs = tables[4]?.records || [];\n  renderRows(allLogs);\n\n  // 3. 検索ボタンのイベントリスナー\n  container.querySelector(\"#btn-search\").addEventListener(\"click\", async () => {\n    const dateStart = container.querySelector(\"#input-date-start\").value;\n    const dateEnd = container.querySelector(\"#input-date-end\").value;\n    const anomalyType = container.querySelector(\"#select-anomaly-type\").value;\n    const workerName = container.querySelector(\"#input-worker-name\").value;\n    const severity = container.querySelector(\"#select-severity\").value;\n    const status = container.querySelector(\"#select-status\").value;\n\n    const rows = await window.aivicApi.list(4);\n    if (rows) {\n      allLogs = rows;\n      let filtered = rows.filter(r => {\n        let match = true;\n        \n        if (dateStart && r.検出日時) {\n          const detectionDate = new Date(r.検出日時).toISOString().split('T')[0];\n          if (detectionDate < dateStart) match = false;\n        }\n        \n        if (dateEnd && r.検出日時) {\n          const detectionDate = new Date(r.検出日時).toISOString().split('T')[0];\n          if (detectionDate > dateEnd) match = false;\n        }\n        \n        if (anomalyType && r.異常値種別 !== anomalyType) match = false;\n        if (workerName && (!r.作業員名 || !r.作業員名.includes(workerName))) match = false;\n        if (severity && r.重要度 !== severity) match = false;\n        if (status && r.確認状況 !== status) match = false;\n        \n        return match;\n      });\n      \n      renderRows(filtered);\n    }\n  });\n\n  // 4. クリアボタン\n  container.querySelector(\"#btn-clear\").addEventListener(\"click\", () => {\n    container.querySelector(\"#input-date-start\").value = \"\";\n    container.querySelector(\"#input-date-end\").value = \"\";\n    container.querySelector(\"#select-anomaly-type\").value = \"\";\n    container.querySelector(\"#input-worker-name\").value = \"\";\n    container.querySelector(\"#select-severity\").value = \"\";\n    container.querySelector(\"#select-status\").value = \"\";\n    renderRows(allLogs);\n  });\n\n  // 5. 詳細表示・更新のイベントデリゲーション\n  container.addEventListener(\"click\", async (e) => {\n    const detailBtn = e.target.closest(\".detail-btn\");\n    const logRow = e.target.closest(\".log-row\");\n    \n    if (detailBtn || (logRow && !e.target.closest(\"button\"))) {\n      const id = detailBtn ? detailBtn.dataset.id : logRow.dataset.id;\n      const log = allLogs.find(l => l.id === id);\n      \n      if (log) {\n        currentLogId = id;\n        container.querySelector(\"#select-detail-status\").value = log.確認状況 || \"未確認\";\n        container.querySelector(\"#input-reviewer\").value = log.確認者名 || \"\";\n        container.querySelector(\"#textarea-memo\").value = log.対応メモ || \"\";\n        detailPanel.classList.remove(\"hidden\");\n      }\n    }\n  });\n\n  // 詳細パネルの操作\n  container.querySelector(\"#btn-update-status\").addEventListener(\"click\", async () => {\n    if (!currentLogId) return;\n    \n    const status = container.querySelector(\"#select-detail-status\").value;\n    const memo = container.querySelector(\"#textarea-memo\").value;\n    \n    const updated = await window.aivicApi.update(4, currentLogId, {\n      確認状況: status,\n      対応メモ: memo,\n      確認者ID: \"current-user-id\", // 実際の実装では現在のユーザーIDを使用\n      確認者名: \"管理者\", // 実際の実装では現在のユーザー名を使用\n      確認日時: new Date().toISOString()\n    });\n    \n    if (updated) {\n      const rows = await window.aivicApi.list(4);\n      if (rows) {\n        allLogs = rows;\n        renderRows(rows);\n      }\n      detailPanel.classList.add(\"hidden\");\n    }\n  });\n\n  container.querySelector(\"#btn-close-detail\").addEventListener(\"click\", () => {\n    detailPanel.classList.add(\"hidden\");\n    currentLogId = null;\n  });\n\n  container.querySelector(\"#btn-view-record\").addEventListener(\"click\", () => {\n    window.showScreen(\"scr-1778907145716\"); // 作業履歴一覧画面\n  });\n\n  // CSVエクスポート\n  container.querySelector(\"#btn-export\").addEventListener(\"click\", async () => {\n    const rows = await window.aivicApi.list(4);\n    if (!rows) return;\n    \n    const headers = [\"検出日時\", \"作業員名\", \"異常値種別\", \"検出項目\", \"検出値\", \"閾値\", \"重要度\", \"確認状況\", \"対応メモ\"];\n    const csvContent = [\n      headers.join(\",\"),\n      ...rows.map(r => [\n        r.検出日時 ? `\"${new Date(r.検出日時).toLocaleString('ja-JP')}\"` : \"\",\n        r.作業員名 ? `\"${r.作業員名}\"` : \"\",\n        r.異常値種別 ? `\"${r.異常値種別}\"` : \"\",\n        r.検出項目 ? `\"${r.検出項目}\"` : \"\",\n        r.検出値 ? `\"${r.検出値}\"` : \"\",\n        r.閾値 ? `\"${r.閾値}\"` : \"\",\n        r.重要度 ? `\"${r.重要度}\"` : \"\",\n        r.確認状況 ? `\"${r.確認状況}\"` : \"\",\n        r.対応メモ ? `\"${r.対応メモ.replace(/\"/g, '\"\"')}\"` : \"\"\n      ].join(\",\"))\n    ].join(\"\\n\");\n    \n    const blob = new Blob([\"\\uFEFF\" + csvContent], { type: \"text/csv;charset=utf-8;\" });\n    const link = document.createElement(\"a\");\n    link.href = URL.createObjectURL(blob);\n    link.download = `異常値検出ログ_${new Date().toISOString().split('T')[0]}.csv`;\n    link.click();\n  });\n}"
}